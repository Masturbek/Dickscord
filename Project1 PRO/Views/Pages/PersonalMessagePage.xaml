<UserControl x:Class="Progect1.Views.Pages.PersonalMessagePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Progect1.ViewModels.Pages"
             mc:Ignorable="d" 
               xmlns:local="clr-namespace:Progect1.ViewModels.Pages"
            Height="423" Width="747"  d:DataContext="{d:DesignInstance {x:Type vm:PersonalMessagePageViewModel}}">
    <Grid>
        <Border>
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" BlurRadius="3"/>
            </Border.Effect>
            <Grid Margin="0 0 -1 385" Background="#FF36393F">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Margin="10 0 0 0" Text="Nickname" Padding="0 5 0 0" Foreground="White" FontFamily="Segoe UI Semibold" FontSize="18" TextAlignment="Center"/>
        </Grid>
        </Border>
        <TextBox Style="{StaticResource ChatControl}" Margin="10 380 10 5"/>
    </Grid>
</UserControl>
